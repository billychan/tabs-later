import React from 'react';
import { cold } from 'react-hot-loader';
import { H3 } from '@blueprintjs/core';

const SavedListsContainer = () => (
  <H3>Save lists</H3>
);

export default cold(SavedListsContainer);
