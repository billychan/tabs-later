import React from 'react';
import { Button, Classes } from '@blueprintjs/core';

const CancelPopoverButton = () => (
  <Button text="Cancel" className={Classes.POPOVER_DISMISS} />
);

export default CancelPopoverButton;
